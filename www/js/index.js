const app = {
  init: function () {
    //this function runs when the app is ready to start
    //get the geolocation coords first
    //after you get the current location build the map
  },
};

document.addEventListener('deviceready', app.init);
